#!/bin/sh -e

if [ "$1" = configure ]; then
    if ! getent passwd sie-dns-sensor >/dev/null; then
        adduser --quiet --system --group --no-create-home --home /var/spool/sie sie-dns-sensor
    fi
fi

update-alternatives --install /usr/bin/nmsgtool nmsgtool /usr/lib/sie-dns-sensor/nmsgtool 0

#DEBHELPER#
